# COPY PASTE CODE NÀY VÀO MAIN.PY

# ============================================================
# BƯỚC 1: THÊM IMPORTS (ở đầu file, cùng với các imports khác)
# ============================================================
from app.db_pool import init_pool, close_pool
from app.db_config import DATABASE_URL


# ============================================================
# BƯỚC 2: TÌM VÀ SỬA LIFESPAN FUNCTION (around line 286)
# ============================================================
# Thêm code khởi tạo pool vào trong lifespan function như sau:

@asynccontextmanager
async def lifespan(app: FastAPI):
    # STARTUP - Thêm code này vào phần startup của bạn
    logger.info("Initializing database connection pool...")
    await init_pool(DATABASE_URL, min_size=5, max_size=20)
    logger.info("✅ Connection pool ready")
    
    yield  # Không thay đổi dòng này
    
    # SHUTDOWN - Thêm code này vào phần shutdown của bạn  
    logger.info("Closing connection pool...")
    await close_pool()
    logger.info("✅ Pool closed")


# SAU KHI THÊM, LƯU FILE → Server tự động reload
